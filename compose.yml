name: ${APP_NAME_HOST}

networks:
  internal:
    name: ${APP_NAME_HOST}_internal
    driver: bridge
    ipam:
      config:
        - subnet: "${APP_NETWORK_POOL}"

volumes:
  habitat-config:
  audiobookshelf-config:
  audiobookshelf-metadata:
  audiobookshelf-provisioning:

secrets:
  AUDIOBOOKSHELF_ROOT_PASSWORD:
    file: ${SECRETS_DIR}/AUDIOBOOKSHELF_ROOT_PASSWORD
  AUDIOBOOKSHELF_OAUTH_CLIENT_ID:
    file: ${SECRETS_DIR}/AUDIOBOOKSHELF_OAUTH_CLIENT_ID
  AUDIOBOOKSHELF_OAUTH_CLIENT_SECRET:
    file: ${SECRETS_DIR}/AUDIOBOOKSHELF_OAUTH_CLIENT_SECRET
  AUDIOBOOKSHELF_OAUTH_CLIENT_SECRET_HASHED_PBKDF2:
    file: ${SECRETS_DIR}/AUDIOBOOKSHELF_OAUTH_CLIENT_SECRET_HASHED_PBKDF2


services:

  habitat-config-echo:
    image: tschebbischeff/habitat-config:latest
    container_name: ${APP_NAME_HOST}_habitat-config-echo
    restart: no
    env_file:
      - ./.env
      - path: ${ENV_DIR}/.env
        required: false
      - path: ${ENV_DIR}/habitat-config.env
        required: false
    environment:
      MODULE_NAME: "echo"
    secrets:
      - AUDIOBOOKSHELF_OAUTH_CLIENT_ID
      - AUDIOBOOKSHELF_OAUTH_CLIENT_SECRET_HASHED_PBKDF2
    networks:
      internal:
    volumes:
      - habitat-config:/habitat-config/orchestration
      - ./habitat-config:/habitat-config/source:ro
      - audiobookshelf-provisioning:/habitat-config/target/audiobookshelf

  audiobookshelf:
    build:
      context: ./audiobookshelf
    container_name: ${APP_NAME_HOST}_audiobookshelf
    restart: unless-stopped
    expose:
      - '80'
    env_file:
      - ./.env
      - path: ${ENV_DIR}/.env
        required: false
      - path: ${ENV_DIR}/audiobookshelf.env
        required: false
    environment:
      SOURCE: 'docker'
      CONFIG_PATH: '/config'
      METADATA_PATH: '/metadata'
      BACKUP_PATH: '/backups'
      PORT: '80'
      ACCESS_TOKEN_EXPIRY: '43200'
      REFRESH_TOKEN_EXPIRY: '604800'
      ALLOW_IFRAME: '1'
      ABS_ROOT_PASSWORD_FILE: '/run/secrets/AUDIOBOOKSHELF_ROOT_PASSWORD'
      ABS_OAUTH_CLIENT_ID_FILE: '/run/secrets/AUDIOBOOKSHELF_OAUTH_CLIENT_ID_FILE'
      ABS_OAUTH_CLIENT_SECRET_FILE: '/run/secrets/AUDIOBOOKSHELF_OAUTH_CLIENT_SECRET_FILE'
      ABS_PROVISIONING_PATH: '/provisioning'
    secrets:
      - AUDIOBOOKSHELF_ROOT_PASSWORD
      - AUDIOBOOKSHELF_OAUTH_CLIENT_ID
      - AUDIOBOOKSHELF_OAUTH_CLIENT_SECRET
    volumes:
      - audiobookshelf-config:/config
      - audiobookshelf-metadata:/metadata
      - audiobookshelf-provisioning:/provisioning
      - ${VOLUME_DIR}/audiobookshelf-audiobooks:/audiobooks
      - ${VOLUME_DIR}/audiobookshelf-podcasts:/podcasts
      - ${VOLUME_DIR}/audiobookshelf-backups:/backups
    networks:
      internal:
    healthcheck:
      test: [ "CMD", "wget", "--spider", "-q", "http://127.0.0.1:80/healthcheck" ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s
    depends_on:
      habitat-config-echo:
        condition: service_completed_successfully
